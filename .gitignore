node_modules/
.next/
out/
build/
.DS_Store
*.local
.env

# AI tooling caches
.aider*
gemini/
.codex
model
.claude/settings.local.json
.agents/
.superpowers/

# Editor / IDE metadata
.obsidian/

# Staging / source images (production assets live in public/)
# Anclado a la raíz: sin la barra inicial el patrón también ignoraba
# public/images/, y cualquier imagen nueva se quedaba fuera del commit.
/images/

# Local audit artifacts
lighthouse-reports/
lighthouse-reports-prod/

# WSL artifacts
*wsl.localhost*
*.Zone.Identifier
*:Zone.Identifier

# Misc logs / scratch
dev.log
cambios.cetra.txt

# TS incremental build info (no versionar)
tsconfig.tsbuildinfo
