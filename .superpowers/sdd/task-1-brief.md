# Task 1: Install Anime.js

**Objective:** Add animejs v4 to project dependencies via npm

**Files:**
- Modify: `package.json`

**Requirements:**
1. Run `npm install animejs` to install v4
2. Verify installation with `npm ls animejs`
3. Commit with message: `git add package.json package-lock.json && git commit -m "deps: install animejs@4.0.0"`

**Expected Result:**
- `package.json` includes `"animejs": "^4.0.0"` (or higher minor/patch)
- `package-lock.json` updated
- Clean commit

**No breaking changes. This is purely additive.**
